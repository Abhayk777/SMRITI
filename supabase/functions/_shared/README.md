# _shared/

Manual mirror of `packages/shared`. Deno cannot resolve npm workspace
packages, so these files are hand-copied, not imported. When
`packages/shared` changes, update the corresponding file here in the
same commit. See AGENTS.md #15.